services:
    api:
        build:
            context: .
        image: atom/api
        ports:
            - "8000:8000"
        environment:
            REDIS_URL: "redis://redis:6379/0"
        depends_on:
            - redis
        restart: unless-stopped

    redis:
        image: redis:8.6.2
        restart: unless-stopped
